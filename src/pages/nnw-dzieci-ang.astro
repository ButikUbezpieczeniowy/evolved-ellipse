<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Child Personal Accident Insurance - Galeria Ubezpieczeniowa</title>
    <meta name="description" content="School Personal Accident (NNW) insurance for children and youth. 24/7 protection, competitive sports, teleconsultations. Diana: 533-488-848 - child NNW specialist">
    
    <script>(function(w,d,s,l,i){w[l]=w[l]||[];w[l].push({'gtm.start':
    new Date().getTime(),event:'gtm.js'});var f=d.getElementsByTagName(s)[0],
    j=d.createElement(s),dl=l!='dataLayer'?'&l='+l:'';j.async=true;j.src=
    'https://www.googletagmanager.com/gtm.js?id='+i+dl;f.parentNode.insertBefore(j,f);
    })(window,document,'script','dataLayer','GTM-T958KD9G');</script>
    <script>
        !function(f,b,e,v,n,t,s){if(f.fbq)return;n=f.fbq=function(){n.callMethod?n.callMethod.apply(n,arguments):n.queue.push(arguments)};if(!f._fbq)f._fbq=n;n.push=n;n.loaded=!0;n.version='2.0';n.queue=[];t=b.createElement(e);t.async=!0;t.src=v;s=b.getElementsByTagName(e)[0];s.parentNode.insertBefore(t,s)}(window,document,'script','https://connect.facebook.net/en_US/fbevents.js');
        fbq('init', '1159782502628502');
        fbq('track', 'PageView');
    </script>
    <noscript><img height="1" width="1" style="display:none" src="https://www.facebook.com/tr?id=1159782502628502&ev=PageView&noscript=1"/></noscript>
    
    <style>
    :root { --primary-color: #dc2626; --secondary-color: #f97316; --text-dark: #1f2937; --text-light: #4b5563; --bg-light: #f9fafb; --white: #ffffff; --border-color: #e5e7eb; }
    body { font-family: system-ui, -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, 'Open Sans', 'Helvetica Neue', sans-serif; margin: 0; background-color: var(--bg-light); color: var(--text-dark); line-height: 1.6; }
    .container { max-width: 900px; margin: 0 auto; padding: 20px; }
    header { background-color: var(--white); text-align: center; padding: 20px; border-bottom: 1px solid var(--border-color); }
    header h1 { color: var(--primary-color); margin: 0; font-size: 2.5rem; }
    header .sub-header { display: flex; justify-content: center; align-items: center; gap: 15px; margin-top: 10px; flex-wrap: wrap; }
    .specialist-info { font-size: 1.1rem; color: var(--text-light); }
    main { padding: 30px 20px; }
    .section { background-color: var(--white); padding: 30px; border-radius: 8px; margin-bottom: 30px; box-shadow: 0 4px 6px rgba(0,0,0,0.05); }
    h2 { color: var(--primary-color); border-bottom: 2px solid var(--secondary-color); padding-bottom: 10px; margin-top: 0; font-size: 2rem; }
    .packages { display: grid; grid-template-columns: repeat(auto-fit, minmax(250px, 1fr)); gap: 20px; margin-top: 20px; }
    .package { border: 1px solid var(--border-color); border-radius: 8px; padding: 20px; text-align: center; transition: transform 0.2s, box-shadow 0.2s; }
    .package:hover { transform: translateY(-5px); box-shadow: 0 8px 15px rgba(0,0,0,0.1); }
    .package h3 { margin: 0 0 10px 0; color: var(--text-dark); }
    .price { font-size: 2rem; font-weight: bold; color: var(--primary-color); margin: 10px 0; }
    .price span { font-size: 1rem; color: var(--text-light); }
    .package ul { list-style: none; padding: 0; margin: 20px 0; text-align: left; }
    .package ul li { margin-bottom: 10px; display: flex; align-items: center; }
    .package ul li::before { content: '✔️'; color: #22c55e; margin-right: 10px; }
    .btn { display: inline-block; background: linear-gradient(90deg, var(--secondary-color), var(--primary-color)); color: var(--white); padding: 12px 25px; border-radius: 50px; text-decoration: none; font-weight: bold; transition: opacity 0.2s; border: none; cursor: pointer; font-size: 1rem; }
    .btn:hover { opacity: 0.9; }
    .why-us ul { list-style: none; padding: 0; }
    .why-us li { font-size: 1.1rem; margin-bottom: 15px; display: flex; align-items: center; }
    .why-us li .icon { font-size: 1.5rem; margin-right: 15px; color: var(--primary-color); }
    .contact-methods { display: flex; justify-content: space-around; flex-wrap: wrap; gap: 20px; margin-top: 30px; }
    .contact-method { text-align: center; }
    .contact-method .icon { font-size: 3rem; margin-bottom: 10px; color: var(--primary-color); }
    .faq-item { margin-bottom: 10px; border-bottom: 1px solid var(--border-color); }
    .faq-question { width: 100%; background: none; border: none; text-align: left; font-size: 1.1rem; font-weight: bold; padding: 15px 0; cursor: pointer; display: flex; justify-content: space-between; align-items: center; }
    .faq-question span { font-size: 1.5rem; color: var(--primary-color); transition: transform 0.2s; }
    .faq-answer { max-height: 0; overflow: hidden; transition: max-height 0.3s ease-out, padding 0.3s ease-out; padding: 0 10px; }
    .faq-answer.active { max-height: 200px; padding: 10px; }
    footer { text-align: center; padding: 20px; font-size: 0.9rem; color: var(--text-light); }
    .language-switcher { margin-top: 15px; }
    .language-switcher a { margin: 0 5px; text-decoration: none; color: var(--primary-color); font-weight: bold; }
    .language-switcher a.active { text-decoration: underline; }
    #sticky-cta-top { position: sticky; top: 0; background-color: rgba(255, 255, 255, 0.95); padding: 15px; z-index: 1000; box-shadow: 0 2px 5px rgba(0,0,0,0.1); backdrop-filter: blur(5px); }
    #sticky-cta-top .container { display: flex; justify-content: space-between; align-items: center; padding: 0; }
    #sticky-cta-top .container > div:first-child { font-weight: bold; }
    #floating-cta-bottom { position: fixed; right: 20px; bottom: 20px; z-index: 1000; }
    #floating-cta-bottom > div { background-color: var(--white); padding: 20px 30px; border-radius: 12px; box-shadow: 0 5px 15px rgba(0,0,0,0.2); max-width: 300px; text-align: center; }
    @media (max-width: 600px) {
        header h1 { font-size: 2rem; }
        h2 { font-size: 1.8rem; }
        .container { padding: 10px; }
        main { padding: 15px 10px; }
        .section { padding: 20px; }
        #sticky-cta-top > .container { flex-direction: column !important; gap: 10px !important; }
        #sticky-cta-top > .container > div:first-child { font-size: 0.9rem !important; }
        #floating-cta-bottom { right: 10px !important; bottom: 10px !important; }
        #floating-cta-bottom > div { max-width: 250px !important; padding: 15px 20px !important; }
    }
</style>